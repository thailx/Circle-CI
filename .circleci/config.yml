version: 2.1

workflows:
  test-env-vars:
    jobs:
      - build

jobs:
  build:
    docker:
      - image: cimg/base:2020.01
    steps:
      - checkout
      - run:
          name: "echo an env var that is part of our project"
          command: |
            return 1
  test_on_fail:
    docker:
      - image: circleci/node:13.8.0
    steps:
      - run:
          name: "Show this page when errors happen"
          command: echo Failed
          when: on_fail